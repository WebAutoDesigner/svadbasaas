generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Заглушка для проверки соединения — будет расширена в Phase 2
model HealthCheck {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
}
